[project]
name = "trustmark-bot"
version = "1.0.0"
description = "TrustMark Telegram reputation, anti-scam and guarantor bot"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

